How to print out network logs during a test
------------------------------------------------

Using Chrome, you can grab the network logs from your application and
log responses recieved.

To run:

`protractor howtos/network/conf.js`
